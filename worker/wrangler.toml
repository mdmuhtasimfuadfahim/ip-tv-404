name = "iptv-worldcup-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

[build]
command = ""

# Optional: custom domain
# routes = [{ pattern = "proxy.yourdomain.com/*", zone_name = "yourdomain.com" }]
